#!/bin/sh

echo "This installs the Debian packages for various Festival voices"

# list voices
#apt-cache search festvox-*

sudo apt-get install festvox-don festvox-rablpc16k festvox-kallpc16k festvox-kdlpc16k
